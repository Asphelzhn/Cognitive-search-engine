import json


String pdf_name
tags = list()
sections = list()
keywords = list()
String full_text


def insert_json(json):


def export_json():

    return json

# Adds whole document to one string, can be a problem with memory management
def add_full_text(fulltext):
    full_text = fulltext


def get_full_text():
    return full_text


def add_tag(tag):
    if tag not in tags
        tags.append(tag)


def remove_tag(tag):
    if tag in tags
        tags.remove(tag)


def get_tags():
    return tags


def add_keyword(keyword):
    if keyword not in keywords
        keywords.append(keyword)


def remove_keyword(keyword):
    if keyword in keywords
        keywords.remove(keyword)


def get_keywords():
    return keywords


def add_pdf_name(pdfname):
    pdf_name = pdfname


def get_pdf_name():
    return pdf_name


def add_section(start, end, section_tags):


def get_sections():